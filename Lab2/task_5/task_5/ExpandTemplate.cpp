#include "ExpandTemplate.h"

using namespace std;

string ExpandTemplate(string const & tpl, map<string, string> const & params)
{
	string newString;
	if (params.empty())
	{
		return tpl;
	}

	return newString;
}